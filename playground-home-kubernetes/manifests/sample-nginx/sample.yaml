apiVersion: v1
kind: Pod
metadata:
  name: nginx-sample
  labels:
    app: nginx-sample
spec:
  containers:
  - image: nginx-sample
    name: nginx-sample
    ports:
    - containerPort: 80
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: ingress-sample
spec:
  ports:
  - port: 80
    name: http
  selector:
    app: nginx-sample
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample
spec:
  ingressClassName: nginx
  rules:
  - host: cafe.example.com
    http:
      paths:
      - path: /tea
        pathType: Prefix
        backend:
          service:
            name: ingress-sample
            port:
              number: 80
      - path: /coffee
        pathType: Prefix
        backend:
          service:
            name: ingress-sample
            port:
              number: 80